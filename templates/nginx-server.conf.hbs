server {
  listen {{ port }};
  listen [::]:{{ port }} ipv6only=on;

  root /gitbooks-dist;
  index index.html;

  server_name {{ domain }};

  {{#each books}}
  location /{{ name}} {
    try_files $uri $uri/ =404;
  }
  {{/each}}
}
